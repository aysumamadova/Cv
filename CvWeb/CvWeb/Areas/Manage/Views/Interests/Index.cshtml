@model List<Interests>
@{
    ViewData["Title"] = "Index";
    int order = 0;
}

<div class="container-fluid">
    <div class="container-fluid d-flex justify-content-between align-items-center">
        <h1>Education:</h1>
        <a asp-controller="Interests" asp-action="Create" class="btn btn-primary">Create</a>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">No</th>
                <th scope="col">Description</th>

            </tr>
        </thead>
        <tbody>
            @foreach (var interests in Model)
            {
                order++;
                <tr>
                    <th scope="row">@order</th>
                    <td>@interests.Desc</td>
                    <td>
                        <form method="post">
                            <a asp-action="Edit" asp-controller="Interests" asp-route-id="@interests.Id" class="btn btn-warning">Edit</a>
                            <button type="submit" asp-action="delete" asp-controller="Interests" asp-route-id="@interests.Id" class="btn btn-danger btn-sweet-delete">Delete</button>
                        </form>

                    </td>
                </tr>
            }

        </tbody>
    </table>
</div>


